{
  "name": "house-rental-app",
  "version": "1.0.0",
  "description": "House Rental Management Application",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run start:dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "start:prod": "npm start --prefix backend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "format": "npm run format --prefix backend && npm run format --prefix frontend",
    "clean": "npm run clean:backend && npm run clean:frontend",
    "clean:backend": "rm -rf backend/dist backend/node_modules",
    "clean:frontend": "rm -rf frontend/dist frontend/node_modules",
    "clean:all": "npm run clean && rm -rf node_modules package-lock.json"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@nestjs/swagger": "^11.4.1",
    "swagger-ui-express": "^5.0.1"
  }
}
