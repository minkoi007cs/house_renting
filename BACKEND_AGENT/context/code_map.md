# BACKEND_AGENT Code map

## Owned (được ghi)
- `BACKEND_AGENT/AGENT.md`, `BACKEND_AGENT/inputs/manifest.md`, `BACKEND_AGENT/outputs/manifest.md`, `BACKEND_AGENT/state/progress.md`, `BACKEND_AGENT/context/code_map.md`
- `backend/**` (code thật agent này chỉnh, tên thư mục agent = BACKEND_AGENT/ để tránh trùng ổ đĩa không phân biệt hoa/thường với backend/).

## Read-only
- `shared/` (research_integrity, tool_conventions, handoff_schema, scope_decisions, glossary)
- `.agents/skills/supabase*` — skill Supabase sẵn có trong repo.

## Out of scope
- Thư mục của agent khác.
